# Activate with: --spring.profiles.active=docker
# Run MySQL first: docker compose up -d
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_management_system?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=hmsroot
